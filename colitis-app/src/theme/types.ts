export type ThemeId = 'light' | 'dark' | 'light-blue';

export interface ThemeColors {
  background: string;
  surface: string;
  textPrimary: string;
  textSecondary: string;
  primary: string;
  accent: string;
  danger: string;
  success: string;
  border: string;
  overlay: string;
}
